(function() {
  var $, $async, $map, ASYNC, CACHE, CND, CUSTOM, D, HELPERS, LINEBREAKER, MACRO_ESCAPER, MD_READER, MKTS, MKTSCRIPT_WRITER, OPTIONS, SEMVER, TEXLIVEPACKAGEINFO, XNCHR, after, alert, badge, before, copy, debug, echo, f, help, hide, info, is_hidden, is_stamped, log, njs_fs, njs_path, options_route, ref, rpr, select, stamp, step, suspend, unstamp, urge, warn, whisper, ƒ,
    slice = [].slice;

  njs_path = require('path');

  njs_fs = require('fs');

  CND = require('cnd');

  rpr = CND.rpr;

  badge = 'mkts/tex-writer';

  log = CND.get_logger('plain', badge);

  info = CND.get_logger('info', badge);

  whisper = CND.get_logger('whisper', badge);

  alert = CND.get_logger('alert', badge);

  debug = CND.get_logger('debug', badge);

  warn = CND.get_logger('warn', badge);

  help = CND.get_logger('help', badge);

  urge = CND.get_logger('urge', badge);

  echo = CND.echo.bind(CND);

  suspend = require('coffeenode-suspend');

  step = suspend.step;

  D = require('pipedreams');

  $ = D.remit.bind(D);

  $async = D.remit_async.bind(D);

  ASYNC = require('async');

  ƒ = CND.format_number.bind(CND);

  HELPERS = require('./helpers');

  TEXLIVEPACKAGEINFO = require('./texlivepackageinfo');

  options_route = '../options.coffee';

  ref = require('./options'), CACHE = ref.CACHE, OPTIONS = ref.OPTIONS;

  SEMVER = require('semver');

  XNCHR = require('./xnchr');

  MKTS = require('./main');

  MKTSCRIPT_WRITER = require('./mktscript-writer');

  MD_READER = require('./md-reader');

  hide = MD_READER.hide.bind(MD_READER);

  copy = MD_READER.copy.bind(MD_READER);

  stamp = MD_READER.stamp.bind(MD_READER);

  unstamp = MD_READER.unstamp.bind(MD_READER);

  select = MD_READER.select.bind(MD_READER);

  is_hidden = MD_READER.is_hidden.bind(MD_READER);

  is_stamped = MD_READER.is_stamped.bind(MD_READER);

  MACRO_ESCAPER = require('./macro-escaper');

  LINEBREAKER = require('./linebreaker');


  /* TAINT experimental, should become part of `PIPEDREAMS` to facilitate automated assembly of pipelines
  based on registered precedences using `CND.TSORT`
   */

  before = function() {
    var i, method, names;
    names = 2 <= arguments.length ? slice.call(arguments, 0, i = arguments.length - 1) : (i = 0, []), method = arguments[i++];
    urge('2134', names);
    return method;
  };

  after = function() {
    var i, method, names;
    names = 2 <= arguments.length ? slice.call(arguments, 0, i = arguments.length - 1) : (i = 0, []), method = arguments[i++];
    urge('7897', names);
    return method;
  };

  this.compile_options = function() {

    /* TAINT this method should go to OPTIONS */
    var cache_locator, cache_route, has_double_slash, has_single_slash, i, len, locator, locators, options_home, options_locator, ref1, route, texinputs_routes;
    options_locator = require.resolve(njs_path.resolve(__dirname, options_route));
    options_home = njs_path.dirname(options_locator);
    this.options = OPTIONS.from_locator(options_locator);
    this.options['home'] = options_home;
    this.options['locator'] = options_locator;
    cache_route = this.options['cache']['route'];
    this.options['cache']['locator'] = cache_locator = njs_path.resolve(options_home, cache_route);
    this.options['xelatex-command'] = njs_path.resolve(options_home, this.options['xelatex-command']);
    if (!njs_fs.existsSync(cache_locator)) {
      this.options['cache']['%self'] = {};
      CACHE.save(this.options);
    }
    this.options['cache']['%self'] = require(cache_locator);
    if ((texinputs_routes = (ref1 = this.options['texinputs']) != null ? ref1['routes'] : void 0) != null) {
      locators = [];
      for (i = 0, len = texinputs_routes.length; i < len; i++) {
        route = texinputs_routes[i];
        has_single_slash = /\/$/.test(route);
        has_double_slash = /\/\/$/.test(route);
        locator = njs_path.resolve(options_home, route);
        if (has_double_slash) {
          locator += '//';
        } else if (has_single_slash) {
          locator += '/';
        }
        locators.push(locator);
      }

      /* TAINT duplication: tex_inputs_home, texinputs_value */

      /* TAINT path separator depends on OS */
      this.options['texinputs']['value'] = locators.join(':');
    }
    return CACHE.update(this.options);
  };

  this.compile_options();

  this.write_mkts_master = function(layout_info, handler) {
    return step((function(_this) {
      return function*(resume) {
        var content_locator, defs, filename, fonts_home, fontspec_version, home, i, len, lines, main_font_name, master_locator, name, newcommands, ref1, ref2, styles, texname, text, use_new_syntax, value, write;
        lines = [];
        write = lines.push.bind(lines);
        master_locator = layout_info['master-locator'];
        content_locator = layout_info['content-locator'];
        help("writing " + master_locator);
        write("");
        write("% " + master_locator);
        write("% do not edit this file");
        write("% generated from " + _this.options['locator']);
        write("% on " + (new Date()));
        write("");
        write("\\documentclass[a4paper,twoside]{book}");
        write("");
        defs = _this.options['defs'];
        write("");
        write("% DEFS");
        if (defs != null) {
          for (name in defs) {
            value = defs[name];
            write("\\def\\" + name + "{" + value + "}");
          }
        }
        newcommands = _this.options['newcommands'];
        write("");
        write("% NEWCOMMANDS");
        if (newcommands != null) {
          for (name in newcommands) {
            value = newcommands[name];
            warn("implicitly converting newcommand value for " + name);
            value = njs_path.resolve(__dirname, '..', value);
            write("\\newcommand{\\" + name + "}{%\n" + value + "%\n}");
          }
        }
        write("");
        write("% PACKAGES");
        write("\\usepackage{mkts2015-consolidated}");
        fontspec_version = (yield TEXLIVEPACKAGEINFO.read_texlive_package_version(_this.options, 'fontspec', resume));
        use_new_syntax = SEMVER.satisfies(fontspec_version, '>=2.4.0');
        fonts_home = _this.options['fonts']['home'];
        write("");
        write("% FONTS");
        write("% assuming fontspec@" + fontspec_version);
        write("\\usepackage{fontspec}");
        ref1 = _this.options['fonts']['files'];
        for (i = 0, len = ref1.length; i < len; i++) {
          ref2 = ref1[i], texname = ref2.texname, home = ref2.home, filename = ref2.filename;
          if (home == null) {
            home = fonts_home;
          }
          if (use_new_syntax) {

            /* TAINT should properly escape values */
            write("\\newfontface{\\" + texname + "}{" + filename + "}[Path=" + home + "/]");
          } else {
            write("\\newfontface\\" + texname + "[Path=" + home + "/]{" + filename + "}");
          }
        }
        write("");
        write("");
        write("% STYLES");
        if ((styles = _this.options['styles']) != null) {
          for (name in styles) {
            value = styles[name];
            write("\\newcommand{\\" + name + "}{%\n" + value + "%\n}");
          }
        }
        main_font_name = _this.options['fonts']['main'];
        if (main_font_name == null) {
          throw new Error("need entry options/fonts/name");
        }
        write("");
        write("% CONTENT");
        write("\\begin{document}" + main_font_name + "\\fontsize{\\myFontsize}{\\myLineheight}\\selectfont");
        write("");
        write("\\input{" + content_locator + "}");
        write("");
        write("\\end{document}");
        text = lines.join('\n');
        return njs_fs.writeFile(master_locator, text, handler);
      };
    })(this));
  };


  /* TAINT should be implemented as separate, document-specific module */

  this.JIZURA = {};

  this.JIZURA.$py = (function(_this) {
    return function(S) {

      /* TAINT should translate special syntax to ordinary commands, then translate to TeX */

      /* TAINT make RegEx more specific, don't include punctuation */
      var compile_pinyin, py_pattern;
      py_pattern = /!py!([^\s]+)/;
      compile_pinyin = function(text) {
        return text.replace(py_pattern, function($0, $1) {

          /* TAINT translate digits to accents */

          /* TAINT consider to use fix_typography_for_tex */
          return "{\\py{}" + $1 + "}";
        });
      };
      return $(function(event, send) {
        var meta, name, text, type;
        type = event[0], name = event[1], text = event[2], meta = event[3];
        if (select(event, '(', 'py')) {
          send(stamp(event));
          return send(['tex', "{\\py{}"]);
        } else if (select(event, ')', 'py')) {
          send(stamp(event));
          return send(['tex', "}"]);
        } else if (select(event, '.', 'text')) {
          return send(['.', 'text', compile_pinyin(text), copy(meta)]);
        } else {
          return send(event);
        }
      });
    };
  })(this);

  this.MKTX = {
    TEX: require('./tex-writer-typofix'),
    DOCUMENT: {},
    COMMAND: {},
    REGION: {},
    BLOCK: {},
    INLINE: {},
    MIXED: {},
    CLEANUP: {}
  };

  this.MKTX.COMMAND.$new_page = (function(_this) {
    return function(S) {
      return $(function(event, send) {
        var meta, name, text, type;
        if (!select(event, '!', 'new-page')) {
          return send(event);
        }
        send(stamp(event));
        type = event[0], name = event[1], text = event[2], meta = event[3];
        return send(['tex', "\\null\\newpage{}"]);
      });
    };
  })(this);

  this.MKTX.COMMAND.$comment = (function(_this) {
    return function(S) {
      var remark;
      remark = MD_READER._get_remark();
      return $(function(event, send) {
        var meta, name, text, type;
        if (!select(event, '.', 'comment')) {
          return send(event);
        }
        type = event[0], name = event[1], text = event[2], meta = event[3];
        return send(remark('drop', "`.comment`: " + (rpr(text)), copy(meta)));
      });
    };
  })(this);

  this.MKTX.DOCUMENT.$begin = (function(_this) {
    return function(S) {
      return $(function(event, send) {
        if (select(event, '(', 'document')) {
          send(stamp(event));
          return send(['tex', "\n% begin of MD document\n"]);
        } else {
          return send(event);
        }
      });
    };
  })(this);

  this.MKTX.DOCUMENT.$end = (function(_this) {
    return function(S) {
      return $(function(event, send) {
        if (select(event, ')', 'document')) {
          send(stamp(event));
          return send(['tex', "\n% end of MD document\n"]);
        } else {
          return send(event);
        }
      });
    };
  })(this);

  this.MKTX.REGION._begin_multi_column = (function(_this) {
    return function() {

      /* TAINT Column count must come from layout / options / MKTS-MD command */

      /* TAINT make `\raggedcolumns` optional? */
      return ['tex', "\n\n\\vspace{\\myLineheight}\\begin{multicols}{2}\\raggedcolumns{}"];
    };
  })(this);

  this.MKTX.REGION._end_multi_column = (function(_this) {
    return function() {
      return ['tex', "\\end{multicols}\n\n"];
    };
  })(this);

  this.MKTX.COMMAND.$multi_column = (function(_this) {
    return function(S) {
      return $(function(event, send) {
        var meta, name, text, type;
        if (select(event, '!', 'multi-column')) {
          type = event[0], name = event[1], text = event[2], meta = event[3];
          send(stamp(hide(['(', '!', name, copy(meta)])));
          send(['(', 'multi-column', text, copy(meta)]);
          send(stamp(hide([')', '!', name, copy(meta)])));
        } else {
          send(event);
        }
        return null;
      });
    };
  })(this);

  this.MKTX.COMMAND.$slash = (function(_this) {
    return function(S) {
      var remark, track;
      track = MD_READER.TRACKER.new_tracker('(multi-column)');
      remark = MD_READER._get_remark();
      return $(function(event, send) {
        var meta, name, text, type, within_multi_column;
        within_multi_column = track.within('(multi-column)');
        track(event);
        if (select(event, '!', 'slash')) {
          type = event[0], name = event[1], text = event[2], meta = event[3];
          send(stamp(event));
          if (within_multi_column) {
            send([')', 'multi-column', null, copy(meta)]);

            /* TAINT consider to send MKTS macro */
            send(['tex', "\\mktsEmptyLine\n"]);
            send(['(', 'multi-column', null, copy(meta)]);
          } else {
            send(remark('drop', "`!slash` because not within `(multi-column)`", copy(meta)));
          }
        } else {
          send(event);
        }
        return null;
      });
    };
  })(this);

  this.MKTX.REGION.$multi_column = (function(_this) {
    return function(S) {
      var remark, track;
      track = MD_READER.TRACKER.new_tracker('(multi-column)');
      remark = MD_READER._get_remark();
      return $(function(event, send) {
        var meta, name, text, type, within_multi_column;
        within_multi_column = track.within('(multi-column)');
        track(event);
        if (select(event, ['(', ')'], 'multi-column')) {
          send(stamp(event));
          type = event[0], name = event[1], text = event[2], meta = event[3];
          if (type === '(') {
            if (within_multi_column) {
              send(remark('drop', "`(multi-column` because already within `(multi-column)`", copy(meta)));
            } else {
              send(track(_this.MKTX.REGION._begin_multi_column()));
            }
          } else {
            if (within_multi_column) {
              send(track(_this.MKTX.REGION._end_multi_column()));
            } else {
              send(remark('drop', "`multi-column)` because not within `(multi-column)`", copy(meta)));
            }
          }
        } else {
          send(event);
        }
        return null;
      });
    };
  })(this);

  this.MKTX.REGION.$single_column = (function(_this) {
    return function(S) {

      /* TAINT consider to implement command `change_column_count = ( send, n )` */
      var remark, track;
      track = MD_READER.TRACKER.new_tracker('(multi-column)');
      remark = MD_READER._get_remark();
      return $(function(event, send) {
        var meta, name, text, type, within_multi_column;
        within_multi_column = track.within('(multi-column)');
        track(event);
        if (select(event, ['(', ')'], 'single-column')) {
          type = event[0], name = event[1], text = event[2], meta = event[3];
          if (type === '(') {
            if (within_multi_column) {
              send(remark('insert', "`multi-column)`", copy(meta)));
              send(track(_this.MKTX.REGION._end_multi_column()));
              send(stamp(event));
            } else {
              send(remark('drop', "`single-column` because not within `(multi-column)`", copy(meta)));
            }
          } else {
            if (within_multi_column) {
              send(stamp(event));
              send(remark('insert', "`(multi-column`", copy(meta)));
              send(track(_this.MKTX.REGION._begin_multi_column()));
            } else {
              send(remark('drop', "`single-column` because not within `(multi-column)`", copy(meta)));
            }
          }
        } else {
          send(event);
        }
        return null;
      });
    };
  })(this);

  this.MKTX.REGION.$code = (function(_this) {
    return function(S) {

      /* TAINT code duplication with `REGION.$keep_lines` possible */
      var track;
      track = MD_READER.TRACKER.new_tracker('(code)');
      return $(function(event, send) {
        var meta, name, text, type, within_code;
        within_code = track.within('(code)');
        track(event);
        if (select(event, '.', 'text')) {
          type = event[0], name = event[1], text = event[2], meta = event[3];
          if (within_code) {
            text = text.replace(/\u0020/g, '\u00a0');
          }
          return send([type, name, text, meta]);
        } else if (select(event, ['(', ')'], 'code')) {
          send(stamp(event));
          type = event[0], name = event[1], text = event[2], meta = event[3];
          if (type === '(') {
            send(['(', 'keep-lines', null, copy(meta)]);
            return send(['tex', "\n\n\\begingroup\\mktsStyleCode{}"]);
          } else {
            send(['tex', "\\endgroup{}{}\n\n"]);
            return send([')', 'keep-lines', null, copy(meta)]);
          }
        } else {
          return send(event);
        }
      });
    };
  })(this);

  this.MKTX.REGION.$keep_lines = (function(_this) {
    return function(S) {
      var track;
      track = MD_READER.TRACKER.new_tracker('(keep-lines)');
      return $(function(event, send) {
        var meta, name, text, type, within_keep_lines;
        within_keep_lines = track.within('(keep-lines)');
        track(event);
        if (select(event, '.', 'text')) {
          type = event[0], name = event[1], text = event[2], meta = event[3];

          /* TAINT other replacements possible; use API */

          /* TAINT U+00A0 (nbsp) might be too wide */
          if (within_keep_lines) {
            text = text.replace(/\u0020/g, '\u00a0');
          }
          return send([type, name, text, meta]);
        } else if (select(event, ['(', ')'], 'keep-lines')) {
          send(stamp(event));
          type = event[0], name = event[1], text = event[2], meta = event[3];
          if (type === '(') {
            track.enter('(keep-lines)');
            return send(['tex', "\\begingroup\\mktsObeyAllLines{}"]);
          } else {
            send(['tex', "\\endgroup{}"]);
            return track.leave('(keep-lines)');
          }
        } else {
          return send(event);
        }
      });
    };
  })(this);

  before('@MKTX.BLOCK.$heading', '@MKTX.COMMAND.$toc', this.MKTX.REGION.$toc = (function(_this) {
    return function(S) {
      var buffer, track;
      track = MD_READER.TRACKER.new_tracker('(toc)');
      buffer = null;
      return $(function(event, send) {
        var meta, name, text, type, within_toc;
        within_toc = track.within('(toc)');
        track(event);
        if (select(event, '(', 'toc')) {
          send(stamp(event));
          type = event[0], name = event[1], text = event[2], meta = event[3];
          return buffer = ['!', name, text, meta];
        } else if (select(event, ')', 'toc')) {
          send(stamp(event));
          if (buffer != null) {
            send(buffer);
            return buffer = null;
          }
        } else if (within_toc && select(event, '.', 'comma')) {
          if (buffer != null) {
            send(buffer);
            return buffer = null;
          }
        } else if (within_toc && select(event, ['(', ')'], 'h')) {
          type = event[0], name = event[1], text = event[2], meta = event[3];
          meta['toc'] = 'omit';
          return send(event);
        } else {
          return send(event);
        }
      });
    };
  })(this));

  this.MKTX.BLOCK.$heading = (function(_this) {
    return function(S) {
      var h_idx, h_nrs, restart_multicols, track;
      restart_multicols = false;
      track = MD_READER.TRACKER.new_tracker('(multi-column)');

      /* TAINT make numbering style configurable */

      /* TAINT generalize for more than 3 levels */
      h_nrs = [1, 1, 1];
      h_idx = -1;
      return $(function(event, send) {
        var h_key, level, meta, name, type, within_multi_column;
        within_multi_column = track.within('(multi-column)');
        track(event);
        if (select(event, '(', 'h')) {
          type = event[0], name = event[1], level = event[2], meta = event[3];
          h_idx += +1;
          h_key = "h-" + h_idx;
          if (meta['h'] == null) {
            meta['h'] = {};
          }
          meta['h']['idx'] = h_idx;
          meta['h']['key'] = h_key;
          if (within_multi_column && level <= 2) {
            send(track(_this.MKTX.REGION._end_multi_column(meta)));
            restart_multicols = true;
          }
          send(['tex', "\n"]);
          send(stamp(event));
          switch (level) {
            case 1:
              send(['tex', "{\\mktsHOne{}"]);
              return send([
                'tex', "\\zlabel{" + h_key + "}", {
                  toc: 'omit'
                }
              ]);
            case 2:
              send(['tex', "{\\mktsHTwo{}"]);
              return send([
                'tex', "\\zlabel{" + h_key + "}", {
                  toc: 'omit'
                }
              ]);
            case 3:
              send(['tex', "{\\mktsHThree{}"]);
              return send([
                'tex', "\\zlabel{" + h_key + "}", {
                  toc: 'omit'
                }
              ]);
            default:
              return send(['.', 'warning', "heading level " + level + " not implemented", copy(meta)]);
          }
        } else if (select(event, ')', 'h')) {
          type = event[0], name = event[1], level = event[2], meta = event[3];
          switch (level) {
            case 1:
              send(['tex', "\\mktsHOneBeg}%\n"]);
              break;
            case 2:
              send(['tex', "\\mktsHTwoBeg}%\n"]);
              break;
            case 3:
              send(['tex', "\\mktsHThreeBeg}%\n"]);
              break;
            default:
              return send(['.', 'warning', "heading level " + level + " not implemented", copy(meta)]);
          }
          send(stamp(event));
          if (restart_multicols) {
            send(track(_this.MKTX.REGION._begin_multi_column(meta)));
            return restart_multicols = false;
          }
        } else {
          return send(event);
        }
      });
    };
  })(this);

  before('@MKTX.COMMAND.$toc', after('@MKTX.BLOCK.$heading', this.MKTX.MIXED.$collect_headings_for_toc = (function(_this) {
    return function(S) {
      var buffer, headings, new_heading, this_heading, within_heading;
      within_heading = false;
      this_heading = null;
      headings = [];
      buffer = [];
      new_heading = function(level, meta) {
        var R;
        R = {
          level: level,
          idx: meta['h']['idx'],
          key: meta['h']['key'],
          events: []
        };
        return R;
      };
      return $(function(event, send) {
        var i, len, level, meta, name, sub_event, text, type;
        type = event[0], name = event[1], text = event[2], meta = event[3];
        if (select(event, '(', 'document')) {
          return send(event);
        } else if (select(event, ')', 'document')) {
          send(unstamp(['.', 'toc-headings', headings, meta]));
          for (i = 0, len = buffer.length; i < len; i++) {
            sub_event = buffer[i];
            send(sub_event);
          }
          buffer.length = 0;
          return send(event);
        } else if ((meta != null) && (meta['toc'] !== 'omit') && select(event, '(', 'h')) {

          /* TAINT use library method to test event category */
          level = text;
          within_heading = true;
          this_heading = new_heading(level, meta);
          headings.push(this_heading);
          return buffer.push(event);
        } else if (select(event, ')', 'h')) {
          within_heading = false;
          this_heading = null;
          return buffer.push(event);
        } else if (within_heading) {

          /* TAINT use library method to determine event category */
          if (event[event.length - 1]['toc'] !== 'omit') {
            if (event.length === 4) {
              this_heading['events'].push([type, name, text, copy(meta)]);
            } else {
              this_heading['events'].push(event);
            }
          }
          if (event[event.length - 1]['toc'] !== 'only') {
            return buffer.push(event);
          }
        } else {
          return buffer.push(event);
        }
      });
    };
  })(this)));

  after('@MKTX.REGION.$toc', '@MKTX.MIXED.$collect_headings_for_toc', this.MKTX.COMMAND.$toc = (function(_this) {
    return function(S) {
      var headings;
      headings = null;
      return $(function(event, send) {

        /* TAINT use library method to test event category */
        var _, events, h_event, heading, i, idx, j, key, last_idx, len, len1, level, meta, name, text, type;
        if (select(event, '.', 'toc-headings')) {
          _ = event[0], _ = event[1], headings = event[2], _ = event[3];
          return send(stamp(event));
        } else if (select(event, '!', 'toc')) {
          send(stamp(event));
          if (headings == null) {
            return send(['.', 'warning', "expecting toc-headings event before this", copy(meta)]);
          }
          type = event[0], name = event[1], text = event[2], meta = event[3];
          send(['tex', '{\\mktsToc%\n']);
          for (i = 0, len = headings.length; i < len; i++) {
            heading = headings[i];
            level = heading.level, events = heading.events, key = heading.key;
            last_idx = events.length - 1;
            for (idx = j = 0, len1 = events.length; j < len1; idx = ++j) {
              h_event = events[idx];

              /* TAINT use library method to determine event category */
              if (h_event.length === 4) {
                h_event = unstamp(h_event);
              }
              if (idx === last_idx) {
                send(['tex', " \\dotfill \\zpageref{" + key + "}"]);
              }
              send(h_event);
            }
          }
          return send(['tex', '\\mktsTocBeg}%\n']);
        } else {
          return send(event);
        }
      });
    };
  })(this));

  this.MKTX.BLOCK.$yadda = (function(_this) {
    return function(S) {
      var generate_yadda, settings, track;
      track = MD_READER.TRACKER.new_tracker('(multi-column)');
      generate_yadda = require('lorem-ipsum');
      settings = {
        count: 1,
        units: 'paragraphs',
        sentenceLowerBound: 5,
        sentenceUpperBound: 15,
        paragraphLowerBound: 3,
        paragraphUpperBound: 7,
        format: 'plain',
        random: CND.get_rnd(42, 3),
        suffix: '\n'
      };
      return $(function(event, send) {
        var meta, name, text, type, within_multi_column, yadda;
        within_multi_column = track.within('(multi-column)');
        track(event);
        if (select(event, '!', 'yadda')) {
          type = event[0], name = event[1], text = event[2], meta = event[3];
          if (within_multi_column) {
            settings['paragraphLowerBound'] = 3;
            settings['paragraphUpperBound'] = 7;
          } else {
            settings['paragraphLowerBound'] = 6;
            settings['paragraphUpperBound'] = 12;
          }
          yadda = generate_yadda(settings);
          yadda = _this.MKTX.TEX.fix_typography_for_tex(yadda, S.options);
          send(stamp(event));
          send(['tex', yadda]);
          return send(['.', 'p', null, copy(meta)]);
        } else {
          return send(event);
        }
      });
    };
  })(this);

  this.MKTX.BLOCK.$paragraph = (function(_this) {
    return function(S) {

      /* TAINT should unify the two observers */
      var track;
      track = MD_READER.TRACKER.new_tracker('(code)', '(keep-lines)');
      return $(function(event, send) {
        var meta, name, text, type, within_code, within_keep_lines;
        within_code = track.within('(code)');
        within_keep_lines = track.within('(keep-lines)');
        track(event);
        if (select(event, '.', 'p')) {
          type = event[0], name = event[1], text = event[2], meta = event[3];
          if (within_code || within_keep_lines) {
            send(stamp(event));
            return send(['tex', '\n\n']);
          } else {
            send(stamp(event));
            return send(_this.MKTX.BLOCK._end_paragraph());
          }
        } else {
          return send(event);
        }
      });
    };
  })(this);

  this.MKTX.BLOCK._end_paragraph = (function(_this) {
    return function() {

      /* TAINT use command from sty */

      /* TAINT make configurable */
      return ['tex', '\n\n'];
    };
  })(this);

  this.MKTX.BLOCK.$unordered_list = (function(_this) {
    return function(S) {
      var item_markup_tex, tex_by_md_markup;
      tex_by_md_markup = {
        '*': '$\\star$',
        'fallback': '—'
      };
      item_markup_tex = null;
      return $(function(event, send) {
        var markup, meta, name, ref1, text, type;
        if (select(event, '(', 'ul')) {
          type = event[0], name = event[1], text = event[2], meta = event[3];
          markup = meta.markup;

          /* TAINT won't work in nested lists */

          /* TAINT make configurable */
          item_markup_tex = (ref1 = tex_by_md_markup[markup]) != null ? ref1 : tex_by_md_markup['fallback'];
          send(stamp(event));
          return send(['tex', '\\begin{itemize}']);
        } else if (select(event, '(', 'li')) {
          send(stamp(event));
          return send(['tex', "\\item[" + item_markup_tex + "] "]);
        } else if (select(event, ')', 'li')) {
          send(stamp(event));
          return send(['tex', '\n']);
        } else if (select(event, ')', 'ul')) {
          send(stamp(event));
          return send(['tex', '\\end{itemize}']);
        } else {
          return send(event);
        }
      });
    };
  })(this);

  before('@MKTX.REGION.$single_column', '@MKTX.REGION.$multi_column', this.MKTX.BLOCK.$hr = (function(_this) {
    return function(S) {
      var remark;
      remark = MD_READER._get_remark();
      return $(function(event, send) {
        var chr, meta, name, text, type;
        if (select(event, '.', 'hr')) {
          type = event[0], name = event[1], text = event[2], meta = event[3];
          switch (chr = text[0]) {
            case '-':
              send(stamp(copy(event)));
              return send(['tex', '\\mktsRulePlain{}']);
            case '*':
              send(stamp(copy(event)));
              return send(['tex', '\\mktsRuleSwell{}']);
            case '#':
              send(['(', 'single-column', null, copy(meta)]);
              send(stamp(copy(event)));
              send(['tex', '\\mktsRuleSwell{}']);
              return send([')', 'single-column', null, copy(meta)]);
            default:
              return send(remark('drop', "`[hr] because markup unknown " + (rpr(text)), copy(meta)));
          }
        } else {
          return send(event);
        }
      });
    };
  })(this));

  this.MKTX.INLINE.$code_span = (function(_this) {
    return function(S) {
      var track;
      track = MD_READER.TRACKER.new_tracker('(code-span)');
      return $(function(event, send) {
        var fragments, meta, name, text, type, within_code_span;
        within_code_span = track.within('(code-span)');
        track(event);
        if (select(event, '(', 'code-span')) {
          send(stamp(event));
          return send(['tex', '{\\mktsStyleCode{}']);
        } else if (select(event, ')', 'code-span')) {
          return send(['tex', "}"]);
        } else if (within_code_span) {
          type = event[0], name = event[1], text = event[2], meta = event[3];
          if (text != null) {
            fragments = LINEBREAKER.fragmentize(text);
            text = fragments.join("\\allowbreak{}");
          }
          return send([type, name, text, meta]);
        } else {
          return send(event);
        }
      });
    };
  })(this);

  this.MKTX.INLINE.$image = (function(_this) {
    return function(S) {
      var alt, alt_cache, event_cache, src, track;
      track = MD_READER.TRACKER.new_tracker('(image)');
      event_cache = [];
      alt_cache = [];
      src = null;
      alt = null;
      return $(function(event, send) {
        var cached_event, i, len, meta, name, text, type, within_image;
        within_image = track.within('(image)');
        track(event);
        type = event[0], name = event[1], text = event[2], meta = event[3];
        if (select(event, '(', 'image')) {
          send(stamp(event));
          return src = njs_path.resolve(S.layout_info['source-home'], meta['src']);
        } else if (select(event, ')', 'image')) {
          alt = alt_cache.join('');
          send(['tex', '\\begin{figure}%\n']);

          /* TAINT escape `src`? */
          send(['tex', "\\includegraphics[width=0.5\\textwidth]{" + src + "}%\n"]);
          send(['tex', "\\caption[" + alt + "]{%\n"]);
          for (i = 0, len = event_cache.length; i < len; i++) {
            cached_event = event_cache[i];
            send(cached_event);
          }
          send(['tex', '}%\n']);
          send(['tex', '\\end{figure}%\n']);
          src = null;
          return alt_cache.length = 0;
        } else if (within_image) {
          event_cache.push(event);
          if (select(event, '.', 'text')) {
            return alt_cache.push(text);
          }
        } else {
          return send(event);
        }
      });
    };
  })(this);

  this.MKTX.MIXED.$raw = (function(_this) {
    return function(S) {
      var remark;
      remark = MD_READER._get_remark();
      return $(function(event, send) {
        var meta, name, text, type;
        if (select(event, '.', 'raw')) {
          type = event[0], name = event[1], text = event[2], meta = event[3];
          send(stamp(hide(copy(event))));
          send(remark('convert', "raw to TeX", copy(meta)));
          text = MACRO_ESCAPER.escape.unescape_escape_chrs(S, text);
          return send(['tex', text]);
        } else {
          return send(event);
        }
      });
    };
  })(this);

  this.MKTX.MIXED.$table = (function(_this) {
    return function(S) {
      var buffer, remark, track;
      track = MD_READER.TRACKER.new_tracker('(table)', '(th)');
      remark = MD_READER._get_remark();
      buffer = null;
      return $(function(event, send) {
        var alignment, col_styles, i, len, meta, name, ref1, text, type, within_table, within_th;
        type = event[0], name = event[1], text = event[2], meta = event[3];
        within_table = track.within('(table)');
        within_th = track.within('(th)');
        track(event);
        if (within_th && select(event, '.', 'text')) {
          send(['(', 'strong', null, copy(meta)]);
          send(stamp(event));
          return send([')', 'strong', null, copy(meta)]);
        } else if (select(event, ')', 'tr')) {
          buffer = null;
          send(stamp(hide(copy(event))));
          return send(['tex', "\\\\\n"]);
        } else {
          if (buffer) {
            send(buffer);
          }
          buffer = null;
          if (select(event, '(', 'table')) {
            send(stamp(hide(copy(event))));
            col_styles = [];
            ref1 = meta['table']['alignments'];
            for (i = 0, len = ref1.length; i < len; i++) {
              alignment = ref1[i];
              switch (alignment) {
                case 'left':
                  col_styles.push('l');
                  break;
                case 'center':
                  col_styles.push('c');
                  break;
                case 'right':
                  col_styles.push('r');
                  break;
                default:
                  col_styles.push('l');
              }
            }
            col_styles = '| ' + (col_styles.join(' | ')) + ' |';
            return send(['tex', "\n\n\\begin{tabular}[pos]{ " + col_styles + " }\n"]);
          } else if (select(event, ')', 'table')) {
            send(stamp(hide(copy(event))));
            return send(['tex', "\\end{tabular}\n\n"]);
          } else if (select(event, '(', 'tbody')) {
            return send(stamp(hide(copy(event))));
          } else if (select(event, ')', 'tbody')) {
            send(['tex', "\\hline\n"]);
            return send(stamp(hide(copy(event))));
          } else if (select(event, '(', 'td')) {
            return send(stamp(hide(copy(event))));
          } else if (select(event, ')', 'td')) {
            send(stamp(hide(copy(event))));
            return buffer = ['tex', " & "];
          } else if (select(event, '(', 'th')) {
            return send(stamp(hide(copy(event))));
          } else if (select(event, ')', 'th')) {
            send(stamp(hide(copy(event))));
            return buffer = ['tex', " & "];
          } else if (select(event, '(', 'thead')) {
            send(['tex', "\\hline\n"]);
            return send(stamp(hide(copy(event))));
          } else if (select(event, ')', 'thead')) {
            send(stamp(hide(copy(event))));
            return send(['tex', "\n\\hline\n"]);
          } else if (select(event, '(', 'tr')) {
            return send(stamp(hide(copy(event))));
          } else {
            return send(event);
          }
        }
      });
    };
  })(this);

  this.MKTX.MIXED.$footnote = (function(_this) {
    return function(S) {

      /* TAINT should move this to initialization */
      var style;
      if (S.footnotes != null) {
        throw new Error("`S.footnotes` already defined");
      }
      S.footnotes = {
        'style': 'on-demand',
        'by-idx': []
      };
      switch (style = S.footnotes['style']) {
        case 'classic':
          return _this.MKTX.MIXED._$footnote_classic(S);
        case 'on-demand':
          return _this.MKTX.MIXED._$footnote_on_demand(S);
        default:
          throw new Error("unknown footnote style " + (rpr(style)));
      }
    };
  })(this);

  this.MKTX.MIXED._$footnote_classic = (function(_this) {
    return function(S) {
      return $(function(event, send) {
        if (select(event, '(', 'footnote')) {
          send(stamp(event));
          return send(['tex', "\\footnote{"]);
        } else if (select(event, ')', 'footnote')) {
          send(stamp(event));
          return send(['tex', "}"]);
        } else {
          return send(event);
        }
      });
    };
  })(this);

  this.MKTX.MIXED._$footnote_on_demand = (function(_this) {
    return function(S) {

      /* TAINT TeX codes used here should be made configurable */
      var cache, current_fn_cache, current_fn_idx, first_fn_idx, insert_footnotes, last_fn_idx, last_was_footnote, remark, track;
      cache = S.footnotes['by-idx'];
      current_fn_idx = -1;
      current_fn_cache = -1;
      first_fn_idx = 0;
      last_fn_idx = -1;
      track = MD_READER.TRACKER.new_tracker('(footnote)');
      remark = MD_READER._get_remark();
      last_was_footnote = false;
      insert_footnotes = function(send, meta) {
        var fn_cache, fn_event, fn_idx, fn_nr, i, j, len, ref1, ref2;
        if (last_fn_idx >= first_fn_idx) {
          send(['tex', "\n\n"]);
          send(['tex', "\\begin{mktsEnNotes}"]);
          for (fn_idx = i = ref1 = first_fn_idx, ref2 = last_fn_idx; ref1 <= ref2 ? i <= ref2 : i >= ref2; fn_idx = ref1 <= ref2 ? ++i : --i) {
            fn_nr = fn_idx + 1;
            fn_cache = cache[fn_idx];
            cache[fn_idx] = null;
            send(['tex', "{\\mktsEnStyleMarkNotes\\mktsEnMarkBefore" + fn_nr + "\\mktsEnMarkAfter{}}"]);
            for (j = 0, len = fn_cache.length; j < len; j++) {
              fn_event = fn_cache[j];
              send(fn_event);
            }
          }
          send(['tex', "\\end{mktsEnNotes}\n\n"]);
          first_fn_idx = last_fn_idx + 1;
          return last_fn_idx = first_fn_idx - 1;
        }
      };
      return $(function(event, send) {
        var fn_nr, fn_separator, meta, name, text, type, within_footnote;
        type = event[0], name = event[1], text = event[2], meta = event[3];
        within_footnote = track.within('(footnote)');
        track(event);
        if (select(event, '(', 'footnote')) {
          send(stamp(event));
          current_fn_cache = [];
          current_fn_idx += +1;
          last_fn_idx = current_fn_idx;
          fn_nr = current_fn_idx + 1;
          cache[current_fn_idx] = current_fn_cache;
          fn_separator = last_was_footnote ? ',' : '';
          return send(['tex', "{\\mktsEnStyleMarkMain{}" + fn_separator + fn_nr + "}"]);
        } else if (select(event, ')', 'footnote')) {
          send(stamp(event));
          current_fn_cache = null;
          return last_was_footnote = true;
        } else if (within_footnote) {
          current_fn_cache.push(event);
          return send(remark('caching', "event within footnote", event));
        } else if (select(event, '!', 'footnotes')) {
          send(stamp(event));
          return insert_footnotes(send, meta);
        } else if (select(event, ')', 'document')) {
          insert_footnotes(send, meta);
          return send(event);
        } else {
          last_was_footnote = false;
          return send(event);
        }
      });
    };
  })(this);

  this.MKTX.MIXED.$footnote.$remove_extra_paragraphs = (function(_this) {
    return function(S) {
      var last_event;
      last_event = null;
      return $(function(event, send, end) {
        if (event != null) {
          if (select(event, ')', 'footnote')) {
            if ((last_event != null) && !select(last_event, '.', 'p')) {
              send(last_event);
            }
            last_event = event;
          } else {
            if (last_event != null) {
              send(last_event);
            }
            last_event = event;
          }
        }
        if (end != null) {
          if (last_event != null) {
            send(last_event);
          }
          return end();
        }
      });
    };
  })(this);

  this.MKTX.INLINE.$translate_i_and_b = (function(_this) {
    return function(S) {
      return $(function(event, send) {
        var meta, name, new_name, text, type;
        if (select(event, ['(', ')'], ['i', 'b'])) {
          type = event[0], name = event[1], text = event[2], meta = event[3];
          new_name = name === 'i' ? 'em' : 'strong';
          return send([type, new_name, text, meta]);
        } else {
          return send(event);
        }
      });
    };
  })(this);

  this.MKTX.INLINE.$mark = (function(_this) {
    return function(S) {
      var mark_idx;
      mark_idx = 0;
      return $(function(event, send) {
        var meta, name, text, type;
        if (select(event, '!', 'mark')) {
          type = event[0], name = event[1], text = event[2], meta = event[3];
          send(stamp(event));
          if (text == null) {
            mark_idx += +1;
            text = "a-" + mark_idx;
          }
          text = _this.MKTX.TEX.fix_typography_for_tex(text, S.options);
          return send(['tex', "\\mktsMark{" + text + "}"]);
        } else {
          return send(event);
        }
      });
    };
  })(this);

  this.MKTX.INLINE.$em_and_strong = (function(_this) {
    return function(S) {
      return $(function(event, send) {
        var meta, name, text, type;
        if (select(event, ['(', ')'], ['em', 'strong'])) {
          send(stamp(event));
          type = event[0], name = event[1], text = event[2], meta = event[3];
          if (type === '(') {
            if (name === 'em') {
              return send(['tex', '{\\mktsStyleItalic{}']);

              /* TAINT must not be sent when in vertical mode */
            } else {
              return send(['tex', '{\\mktsStyleBold{}']);
            }
          } else {
            if (name === 'em') {
              send(['tex', '\\/']);
            }
            return send(['tex', "}"]);
          }
        } else {
          return send(event);
        }
      });
    };
  })(this);

  this.MKTX.INLINE.$link = (function(_this) {
    return function(S) {
      var cache, last_href, track;
      cache = [];
      last_href = null;
      track = MD_READER.TRACKER.new_tracker('(link)');
      return $(function(event, send) {
        var cached_event, i, len, meta, name, text, type, within_link;
        within_link = track.within('(link)');
        track(event);
        type = event[0], name = event[1], text = event[2], meta = event[3];
        if (select(event, '(', 'link')) {
          send(stamp(event));
          return last_href = text;
        } else if (select(event, ')', 'link')) {
          send(stamp(event));
          for (i = 0, len = cache.length; i < len; i++) {
            cached_event = cache[i];
            send(cached_event);
          }
          last_href = _this.MKTX.TEX.fix_typography_for_tex(last_href, S.options);
          send(['(', 'footnote', null, copy(meta)]);
          send(['(', 'url', null, copy(meta)]);
          send(['.', 'text', last_href, copy(meta)]);
          send(['.', 'p', null, copy(meta)]);
          send([')', 'url', null, copy(meta)]);
          send([')', 'footnote', null, copy(meta)]);
          cache.length = 0;
          return last_href = null;
        } else if (within_link) {
          return cache.push(event);
        } else {
          return send(event);
        }
      });
    };
  })(this);

  this.MKTX.INLINE.$url = (function(_this) {
    return function(S) {
      var track;
      track = MD_READER.TRACKER.new_tracker('(url)');
      return $(function(event, send) {
        var fragment, fragments, i, idx, last_idx, len, meta, name, text, type, within_url;
        within_url = track.within('(url)');
        track(event);
        type = event[0], name = event[1], text = event[2], meta = event[3];
        if (select(event, '(', 'url')) {
          send(stamp(event));
          return send(['tex', "{\\mktsStyleUrl{}"]);
        } else if (select(event, ')', 'url')) {
          send(stamp(event));
          return send(['tex', "}"]);
        } else if (within_url && select(event, '.', 'text')) {
          fragments = LINEBREAKER.fragmentize(text);
          last_idx = fragments.length - 1;
          for (idx = i = 0, len = fragments.length; i < len; idx = ++i) {
            fragment = fragments[idx];
            if (idx !== last_idx) {
              if (fragment.endsWith('//')) {
                fragment = fragment.slice(0, +(fragment.length - 3) + 1 || 9e9) + "\\g/\\g/";
              } else if (fragment.endsWith('/')) {
                fragment = fragment.slice(0, +(fragment.length - 2) + 1 || 9e9) + "\\g/";
              }
            }
            fragments[idx] = fragment;
          }
          text = fragments.join("\\g\\allowbreak{}");
          return send([type, name, text, meta]);
        } else {
          return send(event);
        }
      });
    };
  })(this);

  this.MKTX.CLEANUP.$remove_empty_texts = function(S) {
    var remark;
    remark = MD_READER._get_remark();
    return $((function(_this) {
      return function(event, send) {
        var meta, name, text, type;
        if (select(event, '.', 'text')) {
          type = event[0], name = event[1], text = event[2], meta = event[3];
          if (text === '') {

            /* remain silent to make output an easier read */
            null;
            return send(remark('drop', "empty text", copy(meta)));
          } else {
            return send(event);
          }
        } else {
          return send(event);
        }
      };
    })(this));
  };

  this.MKTX.CLEANUP.$drop_empty_p_tags = (function(_this) {
    return function(S) {

      /* TAINT emptyness of  `p` tags ist tested for by counting intermittend `text` events; however, a
      paragraph could conceivably also consist of e.g. a single image.
       */
      var remark, text_count;
      text_count = 0;
      remark = MD_READER._get_remark();
      warn("not using `$drop_empty_p_tags` at the moment");
      return $(function(event, send) {
        return send(event);
      });
    };
  })(this);

  this.MKTX.REGION.$correct_p_tags_before_regions = (function(_this) {
    return function(S) {
      var last_was_begin_document, last_was_p, remark;
      last_was_p = false;
      last_was_begin_document = false;
      remark = MD_READER._get_remark();
      return $(function(event, send) {
        var meta;
        if (select(event, 'tex')) {
          return send(event);
        } else if (select(event, '(', 'document')) {
          last_was_p = false;
          last_was_begin_document = true;
          return send(event);
        } else if (select(event, '.', 'p')) {
          last_was_p = true;
          last_was_begin_document = false;
          return send(event);
        } else if (select(event, ['('])) {
          if ((!last_was_begin_document) && (!last_was_p)) {
            meta = event[event.length - 1];
            send(remark('insert', "`.p` because region or block opens", copy(meta)));
            send(['.', 'p', null, copy(meta)]);
          }
          send(event);
          last_was_p = false;
          return last_was_begin_document = false;
        } else {
          last_was_p = false;
          last_was_begin_document = false;
          return send(event);
        }
      });
    };
  })(this);

  this.MKTX.$show_unhandled_tags = (function(_this) {
    return function(S) {
      return $(function(event, send) {

        /* TAINT selection could be simpler, less repetitive */
        var event_txt, meta, name, ref1, text, type;
        if (((ref1 = event[0]) === 'tex' || ref1 === 'text') || select(event, '.', ['text', 'raw'])) {
          return send(event);
        } else if ((!is_stamped(event)) && (!select(event, '.', 'warning'))) {
          type = event[0], name = event[1], text = event[2], meta = event[3];
          event_txt = "unhandled event: " + (JSON.stringify(event, null, ' '));
          warn(event_txt);
          return send(['.', 'warning', event_txt, copy(meta)]);
        } else {
          return send(event);
        }
      });
    };
  })(this);

  this.MKTX.$show_warnings = (function(_this) {
    return function(S) {
      return $(function(event, send) {

        /* TAINT this makes clear why we should not use '.' as type here; `warning` is a meta-event, not
        primarily a formatting instruction
         */
        var message, meta, name, text, type;
        if (select(event, '.', 'warning')) {
          type = event[0], name = event[1], text = event[2], meta = event[3];
          message = _this.MKTX.TEX.fix_typography_for_tex(text, S.options);

          /* TAINT use location data */
          return send(['tex', "\\begin{mktsEnvWarning}" + message + "\\end{mktsEnvWarning}"]);
        } else {
          return send(event);
        }
      });
    };
  })(this);

  this.$filter_tex = function(S) {

    /* TAINT reduce number of event types, shapes to simplify this */
    return $((function(_this) {
      return function(event, send) {
        if (select(event, 'tex')) {
          return send(event[1]);
        } else if (select(event, '.', ['text', 'raw'])) {
          return send(event[2]);
        } else if (!is_stamped(event)) {
          warn("unhandled event: " + (JSON.stringify(event)));
          return send.error(new Error("unhandled events not allowed at this point; got " + (JSON.stringify(event))));
        }
      };
    })(this));
  };


  /* TAINT makeshift solution for a commands namespace; should be definable by document e.g.
  by `require`ing syntax extension modules
   */

  CUSTOM = {
    JZR: {}
  };


  /*  * # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # */


  /*  * # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # */


  /*  * # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # */

  $map = D._ES.map.bind(D._ES);

  f = function() {
    return this.remit_async_spread = function(method) {
      var $call, $spread, Z, arity, input, output;
      if ((arity = method.length) !== 2) {
        throw new Error("expected a method with an arity of 2, got one with an arity of " + arity);
      }
      Z = [];
      input = D.create_throughstream();
      output = D.create_throughstream();
      $call = (function(_this) {
        return function() {
          return $async(function(event, done) {
            var collect;
            collect = function(data) {
              Z.push(data);
              return null;
            };
            collect.done = function(data) {
              if (data != null) {
                collect(data);
              }
              done(Object.assign([], Z));
              return Z.length = 0;
            };
            method(event, collect);
            return null;
          });
        };
      })(this);
      $spread = (function(_this) {
        return function() {
          return $(function(collection, send, end) {
            var event, i, len;
            if (collection != null) {
              for (i = 0, len = collection.length; i < len; i++) {
                event = collection[i];
                send(event);
              }
            }
            if (end != null) {
              return end();
            }
          });
        };
      })(this);
      input.pipe($call()).pipe($spread()).pipe(output);
      return this.TEE.from_readwritestreams(input, output);
    };
  };

  f.apply(D);


  /*  * # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # */


  /*  * # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # */


  /*  * # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # */

  CUSTOM.JZR.$most_frequent = (function(_this) {
    return function(S) {
      var HOLLERITH, db_route;
      HOLLERITH = require('../../hollerith');
      db_route = njs_path.resolve(__dirname, '../../jizura-datasources/data/leveldb-v2');
      CUSTOM.JZR.db = HOLLERITH.new_db(db_route, {
        create: false
      });
      return D.TEE.from_pipeline([CUSTOM.JZR.$most_frequent.with_fncrs._$rewrite_events(S), CUSTOM.JZR.$most_frequent._$read(S), CUSTOM.JZR.$most_frequent.with_fncrs._$read(S), CUSTOM.JZR.$most_frequent.with_fncrs._$collect(S), CUSTOM.JZR.$most_frequent.with_fncrs._$assemble(S), CUSTOM.JZR.$most_frequent._$assemble(S)]);
    };
  })(this);

  CUSTOM.JZR.$most_frequent.with_fncrs = {};

  CUSTOM.JZR.$most_frequent.with_fncrs._$rewrite_events = (function(_this) {
    return function(S) {
      return $(function(event, send) {
        var _, meta, parameters;
        if (select(event, '!', 'JZR.most_frequent.with_fncrs')) {
          _ = event[0], _ = event[1], parameters = event[2], meta = event[3];
          if (meta['jzr'] == null) {
            meta['jzr'] = {};
          }
          meta['jzr']['group-name'] = 'glyphs-with-fncrs';
          return send(['!', 'JZR.most_frequent', parameters, meta]);
        } else {
          return send(event);
        }
      });
    };
  })(this);

  CUSTOM.JZR.$most_frequent._$read = (function(_this) {
    return function(S) {
      var HOLLERITH_DEMO, defaults;
      HOLLERITH_DEMO = require('../../hollerith/lib/demo');
      defaults = {
        n: 100,
        group_name: 'glyphs'
      };
      return D.remit_async_spread(function(event, send) {
        var group_name, meta, n, name, ref1, ref2, ref3, type;
        if (!select(event, '!', 'JZR.most_frequent')) {
          return send.done(event);
        }
        type = event[0], name = event[1], (ref1 = event[2], n = ref1[0]), meta = event[3];
        if (n == null) {
          n = defaults.n;
        }
        group_name = (ref2 = (ref3 = meta['jzr']) != null ? ref3['group-name'] : void 0) != null ? ref2 : defaults.group_name;
        return step(function*(resume) {
          var error, error1, glyph, glyphs, i, len;
          try {
            glyphs = (yield HOLLERITH_DEMO.read_sample(CUSTOM.JZR.db, n, resume));
          } catch (error1) {
            error = error1;
            warn(error);
            return send.error(error);
          }
          send(stamp(event));
          glyphs = Object.keys(glyphs);
          send(['(', group_name, null, copy(meta)]);
          for (i = 0, len = glyphs.length; i < len; i++) {
            glyph = glyphs[i];
            send(['.', 'glyph', glyph, copy(meta)]);
          }
          send([')', group_name, null, copy(meta)]);
          return send.done();
        });
      });
    };
  })(this);

  CUSTOM.JZR.$most_frequent.with_fncrs._$read = (function(_this) {
    return function(S) {
      var HOLLERITH, track;
      track = MD_READER.TRACKER.new_tracker('(glyphs-with-fncrs)');
      HOLLERITH = require('../../hollerith');
      return D.remit_async_spread(function(event, send) {
        var _, glyph, meta, prefix, within_glyphs;
        within_glyphs = track.within('(glyphs-with-fncrs)');
        track(event);
        if (!(within_glyphs && select(event, '.', 'glyph'))) {
          return send.done(event);
        }
        _ = event[0], _ = event[1], glyph = event[2], meta = event[3];
        prefix = ['spo', glyph];
        return HOLLERITH.read_phrases(CUSTOM.JZR.db, {
          prefix: prefix
        }, function(error, phrases) {
          var i, len, obj, phrase, prd;
          send(event);
          send(['(', 'details', glyph, copy(meta)]);
          for (i = 0, len = phrases.length; i < len; i++) {
            phrase = phrases[i];
            _ = phrase[0], _ = phrase[1], prd = phrase[2], obj = phrase[3];
            send(['*', prd, obj, copy(meta)]);
          }
          send([')', 'details', glyph, copy(meta)]);
          return send.done();
        });
      });
    };
  })(this);

  CUSTOM.JZR.$most_frequent._$assemble = (function(_this) {
    return function(S) {
      var track;
      track = MD_READER.TRACKER.new_tracker('(glyphs)');
      return $(function(event, send) {
        var _, glyph, meta, within_glyphs;
        within_glyphs = track.within('(glyphs)');
        track(event);
        if (select(event, '(', 'glyphs')) {
          return send(stamp(event));
        } else if (within_glyphs && select(event, '.', 'glyph')) {
          _ = event[0], _ = event[1], glyph = event[2], meta = event[3];
          return send(['.', 'text', glyph, copy(meta)]);
        } else if (select(event, ')', 'glyphs')) {
          return send(stamp(event));
        } else {
          return send(event);
        }
      });
    };
  })(this);

  CUSTOM.JZR.$most_frequent.with_fncrs._$collect = (function(_this) {
    return function(S) {
      var collector, this_glyph, track;
      track = MD_READER.TRACKER.new_tracker('(glyphs-with-fncrs)', '(details)');
      this_glyph = null;
      collector = null;
      return $(function(event, send) {
        var _, meta, obj, prd, within_details, within_glyphs;
        within_glyphs = track.within('(glyphs-with-fncrs)');
        within_details = track.within('(details)');
        track(event);
        if (select(event, '(', 'details')) {
          send(stamp(event));
          return collector = {};
        } else if (select(event, ')', 'details')) {
          _ = event[0], _ = event[1], _ = event[2], meta = event[3];
          send(stamp(copy(event)));
          send(['.', 'details', collector, copy(meta)]);
          return collector = null;
        } else if (within_glyphs && select(event, '*')) {
          null;
          _ = event[0], prd = event[1], obj = event[2], _ = event[3];
          return collector[prd] = obj;
        } else {
          return send(event);
        }
      });
    };
  })(this);

  CUSTOM.JZR.$most_frequent.with_fncrs._$assemble = (function(_this) {
    return function(S) {
      var this_glyph, track;
      track = MD_READER.TRACKER.new_tracker('(glyphs-with-fncrs)');
      this_glyph = null;
      return $(function(event, send) {
        var _, details, glyph, i, key, len, meta, ref1, text, value, value_txt, within_glyphs;
        within_glyphs = track.within('(glyphs-with-fncrs)');
        track(event);
        if (select(event, '(', 'glyphs-with-fncrs')) {
          _ = event[0], _ = event[1], this_glyph = event[2], _ = event[3];
          return send(stamp(event));
        } else if (select(event, ')', 'glyphs-with-fncrs')) {
          this_glyph = null;
          return send(stamp(event));
        } else if (within_glyphs && select(event, '.', 'glyph')) {
          _ = event[0], _ = event[1], glyph = event[2], meta = event[3];
          send(['tex', "\\lettrine[lines=3]{\\tfRaise{1.5}"]);
          send(['.', 'text', glyph, copy(meta)]);
          return send(['tex', "}{}"]);
        } else if (within_glyphs && select(event, '.', 'details')) {
          null;
          _ = event[0], _ = event[1], details = event[2], meta = event[3];
          send(['tex', "\\begin{minipage}{0.8\\linewidth}"]);
          ref1 = ['cp/fncr', 'reading/py', 'reading/hi', 'reading/ka', 'reading/gloss'];
          for (i = 0, len = ref1.length; i < len; i++) {
            key = ref1[i];
            value = details[key];
            value_txt = CND.isa_text(value) ? value : rpr(value);
            text = " " + value_txt + " ";
            send(['.', 'text', text, copy(meta)]);
          }
          send(['tex', "\\end{minipage}"]);
          return send(['.', 'p', null, copy(meta)]);
        } else {
          return send(event);
        }
      });
    };
  })(this);

  this.create_tex_write_tee = function(S) {

    /* TAINT get state via return value of MKTS.create_mdreadstream */

    /* TAINT make execution of `$produce_mktscript` a matter of settings */
    var R, mktscript_in, mktscript_out, readstream, settings, writestream;
    readstream = D.create_throughstream();
    writestream = D.create_throughstream();
    mktscript_in = D.create_throughstream();
    mktscript_out = D.create_throughstream();

    /* TAINT need a file to write MKTScript text events to; must still send on incoming events */
    readstream.pipe(CUSTOM.JZR.$most_frequent(S)).pipe(MACRO_ESCAPER.$expand.$remove_backslashes(S)).pipe(this.MKTX.TEX.$fix_typography_for_tex(S)).pipe(this.MKTX.DOCUMENT.$begin(S)).pipe(this.MKTX.DOCUMENT.$end(S)).pipe(this.JIZURA.$py(S)).pipe(this.MKTX.INLINE.$link(S)).pipe(this.MKTX.MIXED.$footnote(S)).pipe(this.MKTX.MIXED.$footnote.$remove_extra_paragraphs(S)).pipe(this.MKTX.COMMAND.$new_page(S)).pipe(this.MKTX.COMMAND.$comment(S)).pipe(this.MKTX.COMMAND.$multi_column(S)).pipe(this.MKTX.COMMAND.$slash(S)).pipe(this.MKTX.MIXED.$table(S)).pipe(this.MKTX.BLOCK.$hr(S)).pipe(this.MKTX.REGION.$multi_column(S)).pipe(this.MKTX.REGION.$single_column(S)).pipe(this.MKTX.REGION.$code(S)).pipe(this.MKTX.REGION.$keep_lines(S)).pipe(this.MKTX.REGION.$toc(S)).pipe(this.MKTX.BLOCK.$heading(S)).pipe(this.MKTX.MIXED.$collect_headings_for_toc(S)).pipe(this.MKTX.COMMAND.$toc(S)).pipe(this.MKTX.BLOCK.$unordered_list(S)).pipe(this.MKTX.INLINE.$code_span(S)).pipe(this.MKTX.INLINE.$url(S)).pipe(this.MKTX.INLINE.$translate_i_and_b(S)).pipe(this.MKTX.INLINE.$em_and_strong(S)).pipe(this.MKTX.INLINE.$image(S)).pipe(this.MKTX.CLEANUP.$drop_empty_p_tags(S)).pipe(this.MKTX.BLOCK.$yadda(S)).pipe(this.MKTX.BLOCK.$paragraph(S)).pipe(this.MKTX.MIXED.$raw(S)).pipe(this.MKTX.CLEANUP.$remove_empty_texts(S)).pipe(MKTSCRIPT_WRITER.$show_mktsmd_events(S)).pipe(this.MKTX.INLINE.$mark(S)).pipe(this.MKTX.$show_unhandled_tags(S)).pipe(this.MKTX.$show_warnings(S)).pipe(this.$filter_tex(S)).pipe(MD_READER.$show_illegal_chrs(S)).pipe(writestream);
    settings = {
      S: S
    };
    R = D.TEE.from_readwritestreams(readstream, writestream, settings);
    if (S['tees'] == null) {
      S['tees'] = {};
    }
    S['tees']['tex-writer'] = R;
    return R;
  };

  this._handle_error = (function(_this) {
    return function(error) {
      var ref1, stack;
      alert(error['message']);
      stack = (ref1 = error['stack']) != null ? ref1 : "(no stacktrace available)";
      whisper('\n' + (stack.split('\n')).slice(0, 11).join('\n'));
      whisper('...');
      return process.exit(1);
    };
  })(this);

  this.pdf_from_md = function(source_route, handler) {

    /* TAINT code duplication */

    /* TAIN only works with docs in the filesystem, not with literal texts */
    f = (function(_this) {
      return function() {
        return step(function*(resume) {
          var S, content_locator, file_output, layout_info, md_input, md_output, md_readstream, md_source, mkscript_locator, mkscript_output, source_locator, tex_input, tex_output, tex_writestream;
          if (handler == null) {
            handler = function() {};
          }
          layout_info = HELPERS.new_layout_info(_this.options, source_route);
          (yield _this.write_mkts_master(layout_info, resume));
          source_locator = layout_info['source-locator'];
          content_locator = layout_info['content-locator'];
          file_output = njs_fs.createWriteStream(content_locator);
          mkscript_locator = layout_info['mkscript-locator'];
          mkscript_output = njs_fs.createWriteStream(mkscript_locator);
          file_output.on('close', function() {
            return HELPERS.write_pdf(layout_info, function(error) {
              if (error != null) {
                throw error;
              }
              if (handler != null) {
                return handler(null);
              }
            });
          });
          S = {
            options: _this.options,
            layout_info: layout_info
          };

          /* TAINT should read MD source stream */
          md_source = njs_fs.readFileSync(source_locator, {
            encoding: 'utf-8'
          });
          md_readstream = MD_READER.create_md_read_tee(md_source);
          tex_writestream = _this.create_tex_write_tee(S);
          md_input = md_readstream.tee['input'];
          md_output = md_readstream.tee['output'];
          tex_input = tex_writestream.tee['input'];
          tex_output = tex_writestream.tee['output'];
          S.resend = md_readstream.tee['S'].resend;
          md_output.pipe(tex_input);
          tex_output.pipe(file_output);
          return md_input.resume();
        });
      };
    })(this);
    return D.run(f, this._handle_error);
  };

  this.tex_from_md = function(md_source, settings, handler) {

    /* TAINT code duplication */
    var $collect_and_call, S, arity, layout_info, md_input, md_output, md_readstream, ref1, source_route, tex_input, tex_output, tex_writestream;
    switch (arity = arguments.length) {
      case 2:
        handler = settings;
        settings = {};
        break;
      case 3:
        null;
        break;
      default:
        throw new Error("expected 2 or 3 arguments, got " + arity);
    }
    $collect_and_call = (function(_this) {
      return function(handler) {
        var Z;
        Z = [];
        return $(function(event, send, end) {
          if (event != null) {
            Z.push(event);
          }
          if (end != null) {
            handler(null, Z.join(''));
            return end();
          }
        });
      };
    })(this);
    source_route = (ref1 = settings['source-route']) != null ? ref1 : '<STRING>';
    layout_info = HELPERS.new_layout_info(this.options, source_route, false);
    S = {
      options: this.options,
      layout_info: layout_info
    };
    md_readstream = MD_READER.create_md_read_tee(md_source);
    tex_writestream = this.create_tex_write_tee(S);
    md_input = md_readstream.tee['input'];
    md_output = md_readstream.tee['output'];
    tex_input = tex_writestream.tee['input'];
    tex_output = tex_writestream.tee['output'];
    S.resend = md_readstream.tee['S'].resend;
    md_output.pipe(tex_input);
    tex_output.pipe($collect_and_call(handler));
    D.run(((function(_this) {
      return function() {
        return md_input.resume();
      };
    })(this)), this._handle_error);
    return null;
  };

  if (module.parent == null) {
    this.pdf_from_md('texts/demo');
  }

}).call(this);

//# sourceMappingURL=../sourcemaps/tex-writer.js.map
