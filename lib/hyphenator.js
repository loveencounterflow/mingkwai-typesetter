// Generated by CoffeeScript 2.3.1
(function() {
  //###########################################################################################################
  var $, CND, D, HOTMETAL, badge, rpr;

  CND = require('cnd');

  rpr = CND.rpr.bind(CND);

  badge = 'MKTS/HYPHENATOR';

  //...........................................................................................................
  HOTMETAL = require('..');

  //...........................................................................................................
  D = require('pipedreams');

  $ = D.remit.bind(D);

  //===========================================================================================================
  // HYPHENATION
  //-----------------------------------------------------------------------------------------------------------
  this.new_hyphenate = function(hyphenation = null, min_length = 2) {
    /* https://github.com/bramstein/hypher */
    var HYPHER, Hypher;
    Hypher = require('hypher');
    if (hyphenation == null) {
      hyphenation = require('hyphenation.en-us');
    }
    HYPHER = new Hypher(hyphenation);
    return HYPHER.hyphenateText.bind(HYPHER);
  };

  //===========================================================================================================
  // STREAMING & PIPING
  //-----------------------------------------------------------------------------------------------------------
  this.$hyphenate = function(hyphenation = null, min_length = 4) {
    var hyphenate;
    hyphenate = this.new_hyphenate(hyphenation, min_length);
    return $((text, send) => {
      return send(hyphenate(text, min_length));
    });
  };

}).call(this);

//# sourceMappingURL=hyphenator.js.map
