// Generated by CoffeeScript 2.3.2
(function() {
  'use strict';
  var CND, CXLTX, P, _, alert, badge, debug, echo, help, info, method_name, rpr, urge, warn, whisper;

  //###########################################################################################################
  CND = require('cnd');

  rpr = CND.rpr;

  badge = 'CXLTX/DEMO';

  debug = CND.get_logger('debug', badge);

  alert = CND.get_logger('alert', badge);

  whisper = CND.get_logger('whisper', badge);

  warn = CND.get_logger('warn', badge);

  help = CND.get_logger('help', badge);

  urge = CND.get_logger('urge', badge);

  info = CND.get_logger('info', badge);

  echo = CND.echo.bind(CND);

  // 4661697 - 2797018

  //-----------------------------------------------------------------------------------------------------------
  this.as_here_x_ref = function(n) {
    var R, last_idx, prefix, suffix;
    n = (parseFloat(n)) / 1864679 * 100;
    R = n.toFixed(0);
    last_idx = R.length - 1;
    prefix = R.slice(0, last_idx);
    suffix = R[last_idx];
    /* TAINT doing `toFixed 1` would have sufficed as such but we want to be able use any construct for the
    decimal */
    return `${prefix}.{}${suffix}`;
  };

  //-----------------------------------------------------------------------------------------------------------
  if (module.parent == null) {
    CXLTX = this;
    [_, _, method_name, ...P] = process.argv;
    echo(CXLTX[method_name](...P));
  }

}).call(this);

//# sourceMappingURL=cxltx-demo.js.map
