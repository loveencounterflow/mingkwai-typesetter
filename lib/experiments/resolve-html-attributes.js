// Generated by CoffeeScript 2.3.2
(function() {
  //###########################################################################################################
  var CND, alert, assign, badge, debug, echo, facets, help, info, log, njs_fs, njs_path, rpr, urge, warn, whisper;

  njs_path = require('path');

  njs_fs = require('fs');

  //...........................................................................................................
  CND = require('cnd');

  rpr = CND.rpr;

  badge = 'MK/TS/MD-READER';

  log = CND.get_logger('plain', badge);

  info = CND.get_logger('info', badge);

  whisper = CND.get_logger('whisper', badge);

  alert = CND.get_logger('alert', badge);

  debug = CND.get_logger('debug', badge);

  warn = CND.get_logger('warn', badge);

  help = CND.get_logger('help', badge);

  urge = CND.get_logger('urge', badge);

  echo = CND.echo.bind(CND);

  //...........................................................................................................
  assign = Object.assign;

  //-----------------------------------------------------------------------------------------------------------
  this._cast_attribute_value = function(x) {
    if (x === '') {
      return null;
    } else {
      return x;
    }
  };

  //-----------------------------------------------------------------------------------------------------------
  this._resolve_html_attribute = function(facets) {
    var facet;
    return assign({}, ...((function() {
      var i, len, results;
      results = [];
      for (i = 0, len = facets.length; i < len; i++) {
        facet = facets[i];
        results.push({
          [`${facet.name}`]: this._cast_attribute_value(facet.value)
        });
      }
      return results;
    }).call(this)));
  };

  //###########################################################################################################
  facets = [
    {
      name: 'frame',
      value: ''
    },
    {
      name: 'illegal-name',
      value: ''
    },
    {
      name: 'foo',
      value: ''
    },
    {
      name: 'bar',
      value: ''
    },
    {
      name: 'thickness',
      value: '1mm'
    }
  ];

  urge(this._resolve_html_attribute(facets));

  urge(this._resolve_html_attribute([]));

}).call(this);

//# sourceMappingURL=resolve-html-attributes.js.map
