// Generated by CoffeeScript 2.3.2
(function() {
  'use strict';
  var $, $async, CND, COLLATZ, FS, PATH, PS, PS2, S, after, alert, assign, badge, copy, debug, defer, echo, help, info, jr, log, provide_collatz, rpr, rprx, select, select_all, send, stamp, urge, warn, whisper,
    modulo = function(a, b) { return (+a % (b = +b) + b) % b; };

  //###########################################################################################################
  PATH = require('path');

  FS = require('fs');

  //...........................................................................................................
  CND = require('cnd');

  rpr = CND.rpr;

  badge = 'MKTS/EXPERIMENTS/CIRCULAR-PIPELINES';

  log = CND.get_logger('plain', badge);

  info = CND.get_logger('info', badge);

  whisper = CND.get_logger('whisper', badge);

  alert = CND.get_logger('alert', badge);

  debug = CND.get_logger('debug', badge);

  warn = CND.get_logger('warn', badge);

  help = CND.get_logger('help', badge);

  urge = CND.get_logger('urge', badge);

  echo = CND.echo.bind(CND);

  // #...........................................................................................................
  // suspend                   = require 'coffeenode-suspend'
  // step                      = suspend.step
  //...........................................................................................................
  // D                         = require 'pipedreams'
  // $                         = D.remit.bind D
  // $async                    = D.remit_async.bind D
  PS = require('pipestreams');

  ({$, $async} = PS);

  assign = Object.assign;

  after = function(dts, f) {
    return setTimeout(f, dts * 1000);
  };

  defer = setImmediate;

  jr = JSON.stringify;

  copy = function(...P) {
    return assign({}, ...P);
  };

  rprx = function(d) {
    var ref, ref1;
    return `${d.sigil} ${d.key}:: ${jr((ref = d.value) != null ? ref : null)} ${jr((ref1 = d.stamped) != null ? ref1 : false)}`;
  };

  // echo '{ ' + ( ( name for name of require './recycle' ).sort().join '\n  ' ) + " } = require './recycle'"
  PS2 = require('./recycle');

  ({select, select_all, stamp} = PS2);

  //-----------------------------------------------------------------------------------------------------------
  provide_collatz = function() {
    //-----------------------------------------------------------------------------------------------------------
    this.new_number_event = function(value, ...other) {
      return PS2.new_single_event('number', value, ...other);
    };
    //-----------------------------------------------------------------------------------------------------------
    this.is_one = function(n) {
      return n === 1;
    };
    this.is_odd = function(n) {
      return modulo(n, 2) !== 0;
    };
    this.is_even = function(n) {
      return modulo(n, 2) === 0;
    };
    //-----------------------------------------------------------------------------------------------------------
    this.$even_numbers = function(S) {
      return $((d, send) => {
        if (!((select(d, '!', 'number')) && (this.is_even(d.value)))) {
          return send(d);
        }
        send(stamp(d));
        send(PS2.recycling(this.new_number_event(d.value / 2, {
          from: d.value
        })));
        return null;
      });
    };
    //-----------------------------------------------------------------------------------------------------------
    this.$odd_numbers = function(S) {
      return $((d, send) => {
        if ((select(d, '!', 'number')) && (!this.is_one(d.value)) && (this.is_odd(d.value))) {
          /* If data event matches condition, stamp it and send it on with new data event: */
          send(stamp(d));
          send(PS2.recycling(this.new_number_event(d.value * 3 + 1, {
            from: d.value
          })));
        } else {
          send(d);
        }
        return null;
      });
    };
    // #-----------------------------------------------------------------------------------------------------------
    // @$odd_numbers = ( S ) ->
    //   return $ ( d, send ) =>
    //     return send d unless ( select d, '!', 'number' ) and ( not @is_one d.value ) and ( @is_odd d.value )
    //     send stamp d
    //     send PS2.recycling ( @new_number_event ( d.value * 3 + 1 ) )

    //-----------------------------------------------------------------------------------------------------------
    this.$skip_known = function(S) {
      var known;
      known = new Set();
      return $((d, send) => {
        if (!select(d, '!', 'number')) {
          return send(d);
        }
        if (known.has(d.value)) {
          return urge('->', d.value);
        }
        send(d);
        return known.add(d.value);
      });
    };
    //-----------------------------------------------------------------------------------------------------------
    this.$terminate = function(S) {
      return $((d, send) => {
        if ((select_all(d, '!', 'number')) && (this.is_one(d.value))) {
          send(stamp(d));
          send(PS2.new_end_event());
        } else {
          send(d);
        }
        return null;
      });
    };
    //-----------------------------------------------------------------------------------------------------------
    this.$throw_on_illegal = function() {
      return PS.$watch(function(d) {
        var type;
        if ((select_all(d, '!', 'number')) && (type = CND.type_of(d.value)) !== 'number') {
          throw new Error(`found an illegal ${type} in ${rpr(d)}`);
        }
        return null;
      });
    };
    //-----------------------------------------------------------------------------------------------------------
    this.$main = function(S) {
      var pipeline;
      pipeline = [];
      // pipeline.push COLLATZ.$skip_known           S
      // pipeline.push PS.$delay 1
      pipeline.push(COLLATZ.$even_numbers(S));
      pipeline.push(COLLATZ.$odd_numbers(S));
      pipeline.push(COLLATZ.$throw_on_illegal(S));
      // pipeline.push COLLATZ.$terminate            S
      return PS.pull(...pipeline);
    };
    //-----------------------------------------------------------------------------------------------------------
    return this;
  };

  COLLATZ = provide_collatz.apply({});

  //-----------------------------------------------------------------------------------------------------------
  this.new_sender = function(S) {
    var R, pipeline;
    S.source = PS2.new_push_source();
    pipeline = [];
    //.........................................................................................................
    pipeline.push(S.source);
    // pipeline.push PS.$watch ( d ) -> urge '37744-1', jr d
    pipeline.push(PS2.$unwrap_recycled());
    // # pipeline.push PS.$delay 0.25
    // pipeline.push PS.$defer()
    // pipeline.push PS.$watch ( d ) -> whisper '37744-2', jr d
    pipeline.push(COLLATZ.$main(S));
    // pipeline.push PS.$watch ( d ) -> if ( select d, '~', 'end' ) then S.source.end()
    pipeline.push(PS2.$recycle(S.source.push));
    // pipeline.push PS.$watch ( d ) -> help jr d
    //.........................................................................................................
    pipeline.push((function() {
      var collector;
      collector = null;
      return $(function(d, send) {
        if (collector == null) {
          collector = [];
        }
        if (select_all(d, '~', 'collect')) {
          send(stamp(d));
          send(PS2.new_event('!', 'numbers', collector));
          collector = null;
        } else if (select_all(d, '!', 'number')) {
          collector.push(d.value);
        } else {
          send(d);
        }
        return null;
      });
    })());
    //.........................................................................................................
    // pipeline.push PS.$watch ( d ) -> help '37744-3', jr d
    //.........................................................................................................
    pipeline.push((function() {
      var collector;
      collector = [];
      return $('null', function(d, send) {
        if (d != null) {
          if (select_all(d, '!', 'number')) {
            return collector.push(d.value);
          } else {
            return send(d);
          }
        } else {
          return send(collector);
        }
      });
    })());
    //.........................................................................................................
    pipeline.push(PS.$watch(function(d) {
      return help('37744-4', jr(d));
    }));
    pipeline.push(PS.$drain(function() {
      return help('ok');
    }));
    PS.pull(...pipeline);
    //.........................................................................................................
    R = function(value) {
      if (CND.isa_number(value)) {
        return S.source.push(PS2.new_event('!', 'number', value));
      } else {
        return S.source.push(value);
      }
    };
    R.end = function() {
      return S.source.end();
    };
    return R;
  };

  //###########################################################################################################
  if (module.parent == null) {
    S = {};
    send = this.new_sender(S);
    urge('-----------');
    send(5);
    send(PS2.new_system_event('collect'));
    send(6);
    send(PS2.new_system_event('collect'));
  }

  // for n in [ 2 .. 3 ]
//   debug n
//   do ( n ) ->
//     send n
//     # defer send n
//     send PS2.new_system_event 'collect'
//     urge '-----------'
//   # # send.end()
// # send PS2.new_system_event 'end'
// send.end()

}).call(this);

//# sourceMappingURL=circular-pipelines.js.map
