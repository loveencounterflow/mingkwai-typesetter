


% ==========================================================================================================
% MKTS PARA-FONT-SIZE
% ----------------------------------------------------------------------------------------------------------


% ----------------------------------------------------------------------------------------------------------
\newdimen\mktsLineheight%
\newdimen\mktsFontsize%
\newdimen\mktsCurrentLineheight%
\newdimen\mktsCurrentFontsize%
\setlength{\mktsFontsize}{3.8mm}%
\setlength{\mktsLineheight}{4.6667mm}%
% \setlength{\mktsLineheight}{5mm}%
\setlength{\mktsCurrentFontsize}{\mktsFontsize}%
\setlength{\mktsCurrentLineheight}{\mktsLineheight}%
\newcommand{\mktsTightParagraphs}{}%
\newcommand{\mktsVspace}[1]{}%
\newcommand{\mktsTabularTopDelta}{}%
\newdimen\mktsTabularLineheightDelta%
\newdimen\mktsTabularLineheightDeltaLast%
\newcommand{\mktsTabularBottomDelta}{}%
% \gdef\mktsNextVspaceCount{1}%


% ==========================================================================================================
%
% ----------------------------------------------------------------------------------------------------------
\DeclareOption{plain}{%
  % \setlength{\baselineskip}{\mktsLineheight}
  % % see http://ftp.fernuni-hagen.de/ftp-dir/pub/mirrors/www.ctan.org/info/l2tabu/german/l2tabu.pdf
  % % for a warning against setting parindent, parskip this way
  % \setlength{\parindent}{0mm}
  % % \setlength{\multicolsep}{6.0pt plus 2.0pt minus 1.5pt}
  % \setlength{\parskip}{\mktsLineheight}

  % thx to http://www.tug.org/TUGboat/tb28-1/tb88bazargan.pdf
  % and http://tex.stackexchange.com/a/86902/28067
  % for the next.
  % \setlength\lineskiplimit{-2.5mm}\relax%
  \setlength\lineskiplimit{-2.5mm}\relax%
  \setlength\lineskip{0mm plus 0mm minus 0mm}%
  \setlength\parskip{\mktsLineheight}%
  % ### TAINT shouldn't this be expressed as a multiple of mktsLineheight? ###
  \setlength\mktsTabularLineheightDelta{-0.13mm}
  \setlength\mktsTabularLineheightDeltaLast{0.1\mktsLineheight}
  \renewcommand{\mktsTabularTopDelta}{0.2}
  \renewcommand{\mktsTabularBottomDelta}{0.45}
  % \lineskiplimit=-10pt%
  % \lineskip=0pt%
  \topskip=\baselineskip%
  % remove vertical space before and after `\begin{tabbing}...\end{tabbing}`
  % thx to http://tex.stackexchange.com/a/16389/28067
  \setlength{\topsep}{0pt}%
  \setlength{\partopsep}{0pt}%
  \setlength{\multicolsep}{0mm}

  \renewcommand{\mktsTightParagraphs}{%
    \setlength\lineskiplimit{-1000mm}%
    \setlength\parskip{0mm}%
    }%

  \renewcommand{\mktsVspace}[1]{%
    \vspace{#1\parskip}%
    % \gdef\mktsNextVspaceCount{1}%
    }

  }


% ==========================================================================================================
%
% ----------------------------------------------------------------------------------------------------------
\DeclareOption{null}{%
  }

% ----------------------------------------------------------------------------------------------------------
\DeclareOption{default}{%
  }


% ----------------------------------------------------------------------------------------------------------
\ExecuteOptions{default}
\ProcessOptions\relax

