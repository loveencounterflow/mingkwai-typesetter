

% ==========================================================================================================
% MKTS HEADERS AND FOOTERS
% ----------------------------------------------------------------------------------------------------------
% \let\footruleskip\undefined\usepackage{fancyhdr}
\usepackage{fancyhdr}
% \usepackage{fix2col}% Patch LaTeX's output routine to handle marks correctly with two columns




% ==========================================================================================================
% JIZURA
% ----------------------------------------------------------------------------------------------------------
% ### TAINT make texts, styling configurable ###
\DeclareOption{jizura}{%
  \pagestyle{fancy}%
  \fancyhf{}% as per https://tex.stackexchange.com/a/110305/128255, clears predefined, avoids nagging about \sl
  \renewcommand{\headrulewidth}{0mm}%
  \renewcommand{\footrulewidth}{0mm}%
  \fancyhead[OR]{{\mktsScaleFont{1}\cjk{}{\cn{}萬字一覽置換索引}}}%
  \fancyhead[EL]{{\mktsScaleFont{1}\cjk{}{\cn{}萬字一覽置換索引}}}%
  % \fancyhead[EL,OR]{\leftmark}%
  % \fancyhead[OL,ER]{\rightmark\cn{{\mktsFontfileBiaukai{}字面萬字一覽}}}%
  \fancyfoot[OR]{\thepage}%
  \fancyfoot[EL]{\thepage}%
  \fancyfoot[OC,EC]{}%
  \fancyfoot[OL,ER]{}%
  }

% ==========================================================================================================
% NULL
% ----------------------------------------------------------------------------------------------------------
\DeclareOption{null}{%
  \pagestyle{empty}%
  }

% ----------------------------------------------------------------------------------------------------------
\DeclareOption{default}{%
  \pagestyle{empty}%
  }


% ----------------------------------------------------------------------------------------------------------
\ExecuteOptions{default}
\ProcessOptions\relax


